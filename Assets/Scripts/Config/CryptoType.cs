/// <summary>
/// Supported cryptocurrency types across all chains (Ethereum, BSC, Polygon).
/// Use this enum instead of raw strings when checking or storing crypto identifiers.
/// </summary>
public enum CryptoType
{
    ETH,
    BNB,
    MATIC,
    SHIB,
    LINK,
    USDT,
    USDC,
    FDUSD,
    CRO,
    APE
}
